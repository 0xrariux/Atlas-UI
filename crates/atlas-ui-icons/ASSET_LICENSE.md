# Atlas icon license

The SVG paths in `assets/` were created specifically for Atlas on 2026-08-08.
They are released under CC0-1.0 and do not come from the historical repository
or a third-party icon set.

Every asset uses a 24 × 24 `viewBox` and a monochrome silhouette so Slint can
apply semantic colors with `Image.colorize`.
