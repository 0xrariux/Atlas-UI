# Architecture checks

Future integration tests in this directory will verify dependency direction,
public facade snapshots, forbidden business vocabulary and arbitrary visual
values. Package-local tests remain beside their implementation.

